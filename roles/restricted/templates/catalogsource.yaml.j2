apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: my-operator-catalog
  namespace: openshift-marketplace
spec:
  sourceType: grpc
  image: {{ registry.host }}:{{ registry.port }}/{{ registry.olm_repo }}/redhat-operators:v1 
  displayName: My Operator Catalog
  publisher: grpc